name: cotizador_supermercado
description: Cotizador colaborativo de precios de supermercado.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^6.8.0
  firebase_core: ^4.13.0
  flutter_riverpod: ^3.2.1
  intl: ^0.20.2

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
